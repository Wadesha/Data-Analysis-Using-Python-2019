# Kaggle 数据集集合 · 迭代版本与实施方法

> 本文档说明站点内容如何靠「联网查询 + 用户反馈」逐版消除「太简陋」，从通用模板套话走向「有出处、有基准、负责任」的深度说明；并沉淀可复用的实施方法。

---

## 一、迭代版本（联网查询驱动）

每一版都明确：上一版卡在哪、这版靠什么（联网核查 / 用户反馈）补上。

### v1.0 静态骨架（已完成）
- 目标：先有可离线打开的纯静态站点（总览 / 过程 / 路线图 / 历史版本）。
- 固化约束：数据不入库；Favicon；互动页以模拟示例呈现，无强制登录。
- 局限：只展示脚本与结构，数据集内容几乎空白。

### v2.0 扩量 + 五段结构（已发布）
- 用 Kaggle API 多类目检索，目录从 26 扩到 **648 / 20 领域**（仅元数据）。
- 详情页五段：介绍 / 分析 / 指导 / 样例 / 过程。
- **根因问题暴露**：五段由「通用模板」生成，远程数据集千篇一律、无真实信息——用户判定「太简陋」。

### v2.1 内容深度升级（已发布）
- 根因修正：把「通用模板」改为「**手写精选 + 生成元数据分离**」架构（生成器不再覆盖手写文件）。
- 13 个本地数据集逐个手写 `local_rich.js`：字段 schema、数据质量、推荐模型、局限与偏差、出处 / 许可证 / 引用（权威来源如 Cortez 2009、UCI、CC BY 4.0）。
- 20 个领域定制模板 `domain_templates.js`，每个领域独立分析要点 / 基线 / 局限。
- 详情页升为**八段卡片**。此轮反复联网调研数据集卡片最佳实践（Datasheets / Data Cards / HuggingFace），并校验关键事实。

### v2.5 真实描述 + 分析结论（当前 / 已发布）
- **联网核查驱动**：直接抓 Kaggle 官方描述，并对经典数据集联网查权威基准数字，替换「套话」。
- `enrich_descriptions.py` 抓取 648 个远程中的 **423 个官方真实描述**（其余无描述 / 403）；前端 markdown 渲染（标题 / 代码 / 列表 / 链接 + 危险协议过滤），长文自动折叠。
- 详情页升为**九段卡片**：新增 ⑨ **分析结果与结论**——13 个本地数据集逐个给出联网核查的经典结论（例：Iris 五折 CV LDA≈0.97、Setosa 完全可分；Mushroom 决策树 100%、odor 主导；Pima RF AUC≈0.88、Glucose 最强；Heart Failure AUC≈0.85），远程回退领域模板分析要点。
- 每个结论标注可复现路径，并与 ⑤ 局限段联动负责任提示（如「高精度 ≠ 好模型」「切勿据此采食」）。

### v3.0 / v3.5（规划）
- 任务 / SOTA 框架（对标 Papers With Code）：数据集 ↔ 基线 ↔ 榜单联动。
- GitHub Pages 部署（仅元数据 + 文档，规避大小限制）；推送前需用户确认，避免额度消耗。

---

## 二、实施方法

### 2.1 总体架构（纯静态、零运行时依赖）
- 站点为静态 HTML/CSS/JS，可离线打开，GitHub Pages 直接托管。
- 数据分层（均不含原始数据集）：
  - `catalog.js`：661 条元数据（648 远程 + 13 本地，20 领域）。
  - `datasets_desc.js`：远程真实描述（Kaggle 抓取，423 条）。
  - `local_rich.js`：13 本地精选（手写、有引用）。
  - `domain_templates.js`：20 领域模板（手写、不覆盖）。
  - `profiles.js`：13 本地 EDA 画像（SVG 图 + 样例行）。
- 生成器三层：`build_catalog.py`（检索元数据）→ `gen_profiles.py`（本地 CSV EDA 出图）→ `gen_site.py`（注入模板生成 catalog.js）。手写文件生成器不覆盖。

### 2.2 内容优先级（根治「套话」）
渲染顺序：**LOCAL_RICH（手写精选）> DATASETS_DESC（Kaggle 官方真实描述）> DOMAIN_TEMPLATES（领域模板兜底）**。
- 本地 13 个 → 用 `local_rich.js` 的字段级内容（最高保真、有出处）。
- 远程有描述 → 用 Kaggle 官方描述（markdown 渲染），不再是模板套话。
- 远程无描述 / 无 local → 回退领域模板，保证每个数据集都有结构化说明、不空白。

### 2.3 安全渲染（防 XSS）
- 描述先**全量 HTML 转义**，再按白名单重建标签（h4–h6、code、ul、安全链接）。
- 链接仅放行 `http(s)` / `mailto` / 相对路径；`javascript:` 等危险协议替换为 `#`。
- 长描述（>2600 字符）折叠：前 2200 字渲染 + 「展开完整描述」按钮。

### 2.4 数据获取与版权
- Kaggle API 仅用于**元数据校验**（凭证本地、`.gitignore` 排除，绝不入库 / 回显）。
- 原始数据不随站点分发；需用时 `kaggle datasets download -d <ref>` 按需拉取，规避 GitHub 100MB 单文件限制、LFS 额度与 Kaggle 版权风险。
- 本地 13 个的字段 / 质量 / 结论均来自公开权威来源，标注许可证（CC0 / CC BY 4.0 等）。

### 2.5 质量保障
- **Node 双分支渲染校验**：用桩 DOM 验证三类分支（本地 / 远程有描述 / 远程无描述）均九段、无 raw markdown、无空占位、远程热度字段非空。每次大改后重跑，防回归。

### 2.6 部署约定
- 托管 GitHub Pages（仅元数据 + 文档，体积小）。
- ⚠️ 每次推送 GitHub / Vercel 触发部署前需用户确认，避免构建额度与资源消耗。
- 历史版本于 `versions.html` 完整归档，纯静态可长期保留全部快照。

---

## 三、如何进一步迭代（给后续自己 / 协作者）
1. 远程 648 个的 ⑨ 分析结论目前靠领域模板兜底——可对其中的热门 / 经典数据集逐个联网核查真实基准，逐步下沉到 `local_rich.js` 或专门的 `datasets_results.js`。
2. Kaggle metadata 未返回 license 字段（全场空）——如需补全许可证，须对 648 个单独调 `kaggle datasets metadata` 再解析，性价比待评估。
3. 引入任务 / SOTA 框架（v3.0）时，复用 ⑨ 段落结构，将「分析结论」升级为「数据集 ↔ 基线 ↔ 榜单」联动。
4. 任何内容修改后，重跑 Node 渲染校验 + 重新生成 `catalog.js` / `profiles.js`，确保静态产物一致。
